# This file has been intentionally emptied.
# It contained dead code using the wrong JWT library (authlib)
# with a hardcoded SECRET_KEY = 'my_secret' different from .env
# and was never imported anywhere in the codebase.
# The active JWT implementation is in app/core/security.py
